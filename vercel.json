{
  "buildCommand": "cd artifacts/betpredict && corepack pnpm install --ignore-scripts && corepack pnpm build",
  "outputDirectory": "artifacts/betpredict/dist/public",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
